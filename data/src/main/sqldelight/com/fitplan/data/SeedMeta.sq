CREATE TABLE seed_meta (
    id      INTEGER NOT NULL PRIMARY KEY CHECK (id = 1),
    version INTEGER NOT NULL
);

selectVersion:
SELECT version
FROM seed_meta
WHERE id = 1;

upsertVersion:
INSERT OR REPLACE INTO seed_meta(id, version)
VALUES (1, ?);
